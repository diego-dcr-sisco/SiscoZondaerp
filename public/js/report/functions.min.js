function showSpinner() {
    $("#fullscreen-spinner").removeClass("d-none");
}

function hideSpinner() {
    $("#fullscreen-spinner").addClass("d-none");
}

function updateOrder() {
    var signatureChanged = $("#signature-changed").val() === '1';
    var signatureData = $("#signature-base64").val() || "";
    var originalSignatureData = $("#signature-original").val() || "";
    var clearSignature = signatureChanged && signatureData === "";
    var sendSignature = signatureChanged && signatureData !== "" && signatureData !== originalSignatureData;
    
    var order = {
        id: parseInt($("#order-id").val()),
        programmed_date: $("#programmed-date").val(),
        completed_date:
            $("#completed-date").val() != ""
                ? $("#completed-date").val()
                : null,
        start_time: $("#start-time").val(),
        end_time: $("#end-time").val() != "" ? $("#end-time").val() : null,
        status: parseInt($("#order-status").val()),
        folio: parseInt($("#folio").val()),
        closed_by: $("#closed-by").val() !== "" ? parseInt($("#closed-by").val()) : null,
        signed_by: $("#signed-by").val(),
        signature_base64: sendSignature ? signatureData : null,
        signature_updated: sendSignature || clearSignature ? 1 : 0,
        clear_signature: clearSignature,
    };

    if (sendSignature && order.signature_base64 && order.signature_base64.length > 0) {
        const base64Size = order.signature_base64.length * 0.75; // Aproximado en bytes
        const maxSize = 3 * 1024 * 1024; // 3MB máximo
        
        if (base64Size > maxSize) {
            alert('La firma es demasiado grande para ser guardada. Por favor, usa una imagen más pequeña.');
            //console.error('Signature too large:', Math.round(base64Size / 1024 / 1024) + 'MB');
            return;
        }
        console.log('Firma modificada, enviando nueva firma de tamaño:', Math.round(order.signature_base64.length / 1024) + 'KB');
    } else if (!sendSignature && !clearSignature) {
        console.log('Firma no modificada, no se enviará al servidor');
    }

    var csrfToken = $('meta[name="csrf-token"]').attr("content");
    
    // Verificar que tenemos el token CSRF
    if (!csrfToken) {
        alert("Error: No se encontró el token CSRF. Por favor, recarga la página.");
        //console.error("CSRF token not found in meta tag");
        return;
    }

    var new_formdata = new FormData();
    new_formdata.append("order", JSON.stringify(order));
    new_formdata.append("_token", csrfToken);

    showSpinner();

    $.ajax({
        type: "POST",
        url: "/report/order/update",
        data: new_formdata,
        headers: {
            "X-CSRF-TOKEN": csrfToken,
        },
        processData: false,
        contentType: false,
        timeout: 30000, // 30 segundos timeout para firmas grandes
        success: function (response) {
            if (response.success) {
                if (sendSignature) {
                    $("#signature-original").val(signatureData);
                }
                if (clearSignature) {
                    $("#signature-original").val("");
                }
                $("#signature-changed").val("0");
                alert("Orden almacenada!");
            } else {
                alert("Error al guardar la orden: " + response.message);
            }
        },
        error: function (xhr, status, error) {
            //console.error("Error details:", xhr.responseText);
            var errorMsg = "Error al actualizar la orden";
            
            if (xhr.status === 403) {
                errorMsg = "Error 403: Acceso denegado. Tu sesión puede haber expirado. Por favor, recarga la página.";
            } else if (xhr.status === 419) {
                errorMsg = "Error 419: Token CSRF expirado. Por favor, recarga la página.";
            } else if (xhr.status === 413 || xhr.status === 0) {
                errorMsg = "Error: La petición es demasiado grande. La firma puede ser muy pesada. Intenta con una imagen más pequeña.";
            } else if (status === 'timeout') {
                errorMsg = "Error: La petición tardó demasiado. La firma puede ser muy pesada.";
            } else if (xhr.responseJSON && xhr.responseJSON.message) {
                errorMsg = xhr.responseJSON.message;
            } else {
                errorMsg += ": " + error;
            }
            
            alert(errorMsg);
        },
        complete: function() {
            hideSpinner();
        }
    });
}

function updateCustomer() {
    var customer = {
        id: parseInt($("#customer-id").val()),
        name: $("#customer-name").val(),
        email:
            $("#customer-email").val() != ""
                ? $("#customer-email").val()
                : null,
        rfc: $("#customer-rfc").val() != "" ? $("#customer-rfc").val() : null,
        address: $("#customer-address").val(),
    };

    var csrfToken = $('meta[name="csrf-token"]').attr("content");
    
    // Verificar que tenemos el token CSRF
    if (!csrfToken) {
        alert("Error: No se encontró el token CSRF. Por favor, recarga la página.");
        //console.error("CSRF token not found in meta tag");
        return;
    }

    var new_formdata = new FormData();
    new_formdata.append("customer", JSON.stringify(customer));
    new_formdata.append("_token", csrfToken);

    showSpinner();

    $.ajax({
        type: "POST",
        url: "/report/customer/update",
        data: new_formdata,
        headers: {
            "X-CSRF-TOKEN": csrfToken,
        },
        processData: false,
        contentType: false,
        success: function (response) {
            if (response.success) {
                alert("Cliente almacenado!");
            } else {
                alert("Error al guardar el cliente: " + response.message);
            }
        },
        error: function (xhr, status, error) {
            //console.error("Error details:", xhr.responseText);
            var errorMsg = "Error al actualizar el cliente";
            
            if (xhr.status === 403) {
                errorMsg = "Error 403: Acceso denegado. Tu sesión puede haber expirado. Por favor, recarga la página.";
            } else if (xhr.status === 419) {
                errorMsg = "Error 419: Token CSRF expirado. Por favor, recarga la página.";
            } else if (xhr.responseJSON && xhr.responseJSON.message) {
                errorMsg = xhr.responseJSON.message;
            } else {
                errorMsg += ": " + error;
            }
            
            alert(errorMsg);
        },
        complete: function() {
            hideSpinner();
        }
    });
}

function updateDescription(service_id) {
    const rawHtml = $(`#service${service_id}-text`).summernote("code");

    // DOMPDF respeta mejor width en atributo que solo en estilos inline.
    const normalizedHtml = normalizeImageSizesForPdf(rawHtml);

    var description = {
        service_id: parseInt(service_id),
        text: normalizedHtml,
        can_propagate: $(`#service${service_id}-can-propagate`).is(":checked"),
        order_id: parseInt($("#order-id").val()),
    };

    var csrfToken = $('meta[name="csrf-token"]').attr("content");
    
    // Verificar que tenemos el token CSRF
    if (!csrfToken) {
        alert("Error: No se encontró el token CSRF. Por favor, recarga la página.");
        //console.error("CSRF token not found in meta tag");
        return;
    }

    var new_formdata = new FormData();
    new_formdata.append("description", JSON.stringify(description));
    new_formdata.append("_token", csrfToken);

    showSpinner();

    $.ajax({
        type: "POST",
        url: "/report/description/update",
        data: new_formdata,
        headers: {
            "X-CSRF-TOKEN": csrfToken,
        },
        processData: false,
        contentType: false,
        success: function (response) {
            if (response.success) {
                alert("Descripción actualizada!");
            } else {
                alert("Error al actualizar la descripción: " + response.message);
            }
        },
        error: function (xhr, status, error) {
            //console.error("Error details:", xhr.responseText);
            var errorMsg = "Error al actualizar la descripción";
            
            if (xhr.status === 403) {
                errorMsg = "Error 403: Acceso denegado. Tu sesión puede haber expirado. Por favor, recarga la página.";
            } else if (xhr.status === 419) {
                errorMsg = "Error 419: Token CSRF expirado. Por favor, recarga la página.";
            } else if (xhr.responseJSON && xhr.responseJSON.message) {
                errorMsg = xhr.responseJSON.message;
            } else {
                errorMsg += ": " + error;
            }
            
            alert(errorMsg);
        },
        complete: function() {
            hideSpinner();
        }
    });
}

function normalizeImageSizesForPdf(html) {
    if (!html) return html;

    return html.replace(/<img\b[^>]*>/gi, function(imgTag) {
        const styleMatch = imgTag.match(/style\s*=\s*(["'])(.*?)\1/i);
        const styleValue = styleMatch && styleMatch[2] ? styleMatch[2] : "";
        const widthMatch = styleValue.match(/width\s*:\s*([0-9]+(?:\.[0-9]+)?)\s*(px|%)/i);
        const sanitizedStyle = styleValue
            .split(";")
            .map(function(rule) {
                return rule.trim();
            })
            .filter(function(rule) {
                return rule && !/^(width|height|max-width|max-height|min-width|min-height|object-fit)\s*:/i.test(rule);
            });

        sanitizedStyle.push("max-width: 100%");
        sanitizedStyle.push("height: auto");
        sanitizedStyle.push("object-fit: contain");
        sanitizedStyle.push("display: block");

        let normalizedTag = imgTag
            .replace(/\sheight\s*=\s*(["'])[^"']*\1/ig, "")
            .replace(/\sheight\s*=\s*[^\s>]+/ig, "");

        if (widthMatch) {
            const widthValue = Math.round(parseFloat(widthMatch[1]));
            const unit = widthMatch[2].toLowerCase();

            if (unit === "px" && !Number.isNaN(widthValue) && widthValue > 0) {
                if (/\swidth\s*=\s*["'][^"']*["']/i.test(normalizedTag)) {
                    normalizedTag = normalizedTag.replace(/(\swidth\s*=\s*["'])[^"']*(["'])/i, `$1${widthValue}$2`);
                } else {
                    normalizedTag = normalizedTag.replace("<img", `<img width="${widthValue}"`);
                }
            }
        }

        if (styleMatch) {
            return normalizedTag.replace(/style\s*=\s*(["'])(.*?)\1/i, `style="${sanitizedStyle.join("; ")}"`);
        }

        return normalizedTag.replace("<img", `<img style="${sanitizedStyle.join("; ")}"`);
    });
}

// Función auxiliar para comprimir imágenes base64 en el HTML
function compressBase64Images(html) {
    if (!html) return html;
    
    // Buscar todas las imágenes base64
    const imgRegex = /<img[^>]+src="data:image\/(png|jpeg|jpg|gif);base64,([^"]+)"[^>]*>/gi;
    
    return html.replace(imgRegex, function(match, format, base64Data) {
        try {
            // Si la imagen es muy grande (>500KB en base64), comprimirla
            if (base64Data.length > 500000) {
                console.log('Comprimiendo imagen grande...');
                
                // Crear un canvas temporal para comprimir
                const img = new Image();
                const canvas = document.createElement('canvas');
                const ctx = canvas.getContext('2d');
                
                img.onload = function() {
                    // Reducir tamaño si es muy grande
                    let width = img.width;
                    let height = img.height;
                    const maxSize = 1200; // máximo 1200px en cualquier dimensión
                    
                    if (width > maxSize || height > maxSize) {
                        if (width > height) {
                            height = (height / width) * maxSize;
                            width = maxSize;
                        } else {
                            width = (width / height) * maxSize;
                            height = maxSize;
                        }
                    }
                    
                    canvas.width = width;
                    canvas.height = height;
                    ctx.drawImage(img, 0, 0, width, height);
                    
                    // Comprimir a JPEG con calidad 0.7
                    return canvas.toDataURL('image/jpeg', 0.7);
                };
                
                img.src = 'data:image/' + format + ';base64,' + base64Data;
            }
            
            return match; // Retornar sin cambios si no es muy grande
        } catch (e) {
            //console.error('Error al comprimir imagen:', e);
            return match; // Retornar original si hay error
        }
    });
}

function updateNotes() {
    var notesHtml = $("#order-notes").summernote("code");
    
    // Comprimir imágenes si son muy grandes
    notesHtml = compressBase64Images(notesHtml);
    
    var notes = {
        text: notesHtml,
        order_id: parseInt($("#order-id").val()),
    };

    var csrfToken = $('meta[name="csrf-token"]').attr("content");
    
    // Verificar que tenemos el token CSRF
    if (!csrfToken) {
        alert("Error: No se encontró el token CSRF. Por favor, recarga la página.");
        //console.error("CSRF token not found in meta tag");
        return;
    }

    var new_formdata = new FormData();
    new_formdata.append("notes", JSON.stringify(notes));
    new_formdata.append("_token", csrfToken);

    showSpinner();

    $.ajax({
        type: "POST",
        url: "/report/notes/update",
        data: new_formdata,
        headers: {
            "X-CSRF-TOKEN": csrfToken,
        },
        processData: false,
        contentType: false,
        success: function (response) {
            if (response.success) {
                $('#notes').val(notes.text);
                alert("Notas actualizada!");
            } else {
                alert("Error al actualizar las notas: " + response.message);
            }
        },
        error: function (xhr, status, error) {
            //console.error("Error details:", xhr.responseText);
            var errorMsg = "Error al actualizar las notas";
            
            if (xhr.status === 403) {
                errorMsg = "Error 403: Acceso denegado. Tu sesión puede haber expirado. Por favor, recarga la página.";
            } else if (xhr.status === 419) {
                errorMsg = "Error 419: Token CSRF expirado. Por favor, recarga la página.";
            } else if (xhr.responseJSON && xhr.responseJSON.message) {
                errorMsg = xhr.responseJSON.message;
            } else {
                errorMsg += ": " + error;
            }
            
            alert(errorMsg);
        },
        complete: function() {
            hideSpinner();
        }
    });
}

function deleteSignature() {
    if (!confirm("¿Está seguro de que desea eliminar la firma? Esta acción no se puede deshacer.")) {
        return;
    }

    var order = {
        id: parseInt($("#order-id").val()),
        programmed_date: $("#programmed-date").val(),
        completed_date: $("#completed-date").val() != "" ? $("#completed-date").val() : null,
        start_time: $("#start-time").val(),
        end_time: $("#end-time").val() != "" ? $("#end-time").val() : null,
        status: parseInt($("#order-status").val()),
        closed_by: $("#closed-by").val() !== "" ? parseInt($("#closed-by").val()) : null,
        signed_by: "",
        signature_base64: "",
        signature_updated: 1,
        clear_signature: true,
    };

    var csrfToken = $('meta[name="csrf-token"]').attr("content");
    
    // Verificar que tenemos el token CSRF
    if (!csrfToken) {
        alert("Error: No se encontró el token CSRF. Por favor, recarga la página.");
        //console.error("CSRF token not found in meta tag");
        return;
    }

    var new_formdata = new FormData();
    new_formdata.append("order", JSON.stringify(order));
    new_formdata.append("_token", csrfToken);

    console.log('Eliminando firma de la orden');

    showSpinner();

    $.ajax({
        type: "POST",
        url: "/report/order/update",
        data: new_formdata,
        headers: {
            "X-CSRF-TOKEN": csrfToken,
        },
        processData: false,
        contentType: false,
        timeout: 10000, // 10 segundos timeout
        success: function (response) {
            if (response.success) {
                // Limpiar la firma en el frontend
                $("#signed-by").val("");
                $("#signature-base64").val("");
                $("#signature-original").val("");
                $("#signature-changed").val("0");
                $("#signature-preview").attr("src", "");
                $("#signature-preview").hide();
                
                alert("Firma eliminada correctamente!");
                location.reload(); // Recargar para reflejar los cambios
            } else {
                alert("Error al eliminar la firma: " + response.message);
            }
        },
        error: function (xhr, status, error) {
            //console.error("Error details:", xhr.responseText);
            var errorMsg = "Error al eliminar la firma";
            
            if (xhr.status === 403) {
                errorMsg = "Error 403: Acceso denegado. Tu sesión puede haber expirado. Por favor, recarga la página.";
            } else if (xhr.status === 419) {
                errorMsg = "Error 419: Token CSRF expirado. Por favor, recarga la página.";
            } else if (status === 'timeout') {
                errorMsg = "Error: La petición tardó demasiado. Intenta nuevamente.";
            } else if (xhr.responseJSON && xhr.responseJSON.message) {
                errorMsg = xhr.responseJSON.message;
            } else {
                errorMsg += ": " + error;
            }
            
            alert(errorMsg);
        },
        complete: function() {
            hideSpinner();
        }
    });
}
